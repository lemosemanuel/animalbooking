import 'package:flutter/material.dart';

class WalkersScreen extends StatelessWidget {
   
  const WalkersScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('WalkersScreen'),
      ),
    );
  }
}